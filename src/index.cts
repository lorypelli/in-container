import {
    async,
    sync,
    inContainerAsync,
    inDockerAsync,
    inPodmanAsync,
    inContainerSync,
    inDockerSync,
    inPodmanSync,
} from './shared.js';

const container = {
    async,
    sync,
    inContainerAsync,
    inDockerAsync,
    inPodmanAsync,
    inContainerSync,
    inDockerSync,
    inPodmanSync,
};

export = container;
